name: Publish to Docker Hub
env:
  DOCKER_IMAGE: elssdu/jwt-forward-auth
on:
  push:
    tags:
      - "release-*"
    branches:
      - master
      - "release-*"
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Log in with Docker Hub
        run: echo "$DOCKER_PASSWORD" | docker login -u $DOCKER_USERNAME --password-stdin
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build docker image
        run: docker build -t $DOCKER_IMAGE .

      - name: Push to latest
        if: github.ref == 'refs/heads/master'
        run: docker tag $DOCKER_IMAGE $DOCKER_IMAGE:latest && docker push $DOCKER_IMAGE:latest

      - name: Determine release tag
        if: startsWith(github.ref, 'refs/tags/release-')
        run: echo ::set-env name=RELEASE_TAG::${GITHUB_REF/refs\/tags\/release-/}
      - name: Push to release tag
        if: startsWith(github.ref, 'refs/tags/release-')
        run: docker tag $DOCKER_IMAGE $DOCKER_IMAGE:$RELEASE_TAG && docker push $DOCKER_IMAGE:$RELEASE_TAG

      - name: Determine release branch
        if: startsWith(github.ref, 'refs/heads/release-')
        run: echo ::set-env name=RELEASE_BRANCH::${GITHUB_REF/refs\/heads\/release-/}
      - name: Push to release branch
        if: startsWith(github.ref, 'refs/heads/release-')
        run: docker tag $DOCKER_IMAGE $DOCKER_IMAGE:$RELEASE_BRANCH && docker push $DOCKER_IMAGE:$RELEASE_BRANCH
